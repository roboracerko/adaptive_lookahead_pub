# Map Setup Guide

To add a new map for local experiments:

1. place the map image and YAML metadata in the simulator package
2. add the corresponding raceline CSV
3. create a scenario YAML for the benchmark suite
4. point the controller config at the new raceline

The anonymized review package is configured around the bundled benchmark maps and does not require extra setup for the included experiments.
